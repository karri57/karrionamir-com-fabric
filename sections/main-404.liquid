<div class="page-width">
  <div class="empty-state">
    <h1 class="empty-state__title">{{ '404.title' | t }}</h1>
    <p>{{ '404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="button" style="margin-top: 1.5rem;">{{ '404.link' | t }}</a>
  </div>
</div>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
