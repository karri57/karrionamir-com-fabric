<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {%- render 'meta-tags' -%}
    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    {%- render 'css-variables' -%}
    {{ content_for_header }}
  </head>
  <body>
    <div class="page-width" style="min-height: 100svh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center;">
      {{ content_for_layout }}
    </div>
  </body>
</html>
